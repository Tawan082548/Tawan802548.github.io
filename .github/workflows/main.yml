name: Say Hello on Push

on: [push]

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Say Hello
        run: echo "👋 สวัสดีจาก GitHub Actions!"
